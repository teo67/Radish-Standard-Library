d FUNCTION p {
    ;Execute a function in Radish by passing in an array of arguments. The array will be dissected such that each item becomes a single argument in the function call.
    @args@The array of arguments to be passed in to the function.@;
    d call p t(args p []) {
        h CALL(this, args)
    }
}
h FUNCTION