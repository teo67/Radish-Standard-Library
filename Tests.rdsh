d Tests p class {
    d constructor p t(_name, _caseNames, _cases, _len) {
        this.name p _name
        this.caseNames p _caseNames
        this.cases p _cases
        this.len p _len # not automated in order to avoid using stdlib #
    }
    d execute p t() {
        d good p yes
        for(d i p 0, i < this.len, i++) {
            if(this.cases[i]()) {
                holler("'this.name': 'this.caseNames[i]' passed!")
            } else {
                holler("'this.name': 'this.caseNames[i]' failed!")
                good p no
            }
        }
        if(!good) {
            holler("'this.name' FAIL")
        }
        h good
    }
}
h Tests