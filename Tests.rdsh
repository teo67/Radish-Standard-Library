d Tests p class {
    constructor p t(_caseNames p [], _cases p [], _len p 0) {
        this.caseNames p _caseNames
        this.cases p _cases
        this.len p _len # not automated in order to avoid using stdlib #
    }
    d execute p t() {
        d good p yes
        for(d i p 0, i < this.len, i++) {
            holler("starting 'this.caseNames[i]'...")
            if(this.cases[i]()) {
                holler("'this.caseNames[i]' passed!")
            } else {
                holler("'this.caseNames[i]' FAILED!")
                good p no
            }
        }
        h good
    }
}
h Tests