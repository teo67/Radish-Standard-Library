d primes p [2]
h t(input p 2) { 
    d last p primes[primes.length - 1]
    if(input <= last) {
        h primes.has(input)
    }
    for(d n p 1 + last, primes[primes.length - 1] < input, n++) {
        d ip p yes
        for(d i p 0, i < primes.length, i++) {
            if(n % primes[i] == 0) {
                ip p no
                cancel
            }
        }
        if(ip) {
            primes[primes.length] p n
        }
    }
    h primes[primes.length - 1] == input
} 